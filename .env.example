# 이 파일을 복사해서 `.env.local` 로 만들고, 실제 키를 채워 넣으세요.
#   cp .env.example .env.local
# `.env.local` / `.env` 는 .gitignore 로 저장소에 올라가지 않습니다. 절대 커밋하지 마세요.

# Exa 웹 검색 API 키 — https://exa.ai 에서 발급
EXA_API_KEY=

# Filesystem MCP 가 접근할 프로젝트 디렉터리 (절대경로)
FILESYSTEM_DIR=
